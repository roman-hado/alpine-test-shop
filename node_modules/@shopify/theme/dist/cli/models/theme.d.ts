export declare class Theme {
    id: number;
    name: string;
    createdAt: string;
    updatedAt: string;
    private _role;
    themeStoreId: number | null;
    previewable: boolean;
    processing: boolean;
    adminGraphqlApiId: string;
    constructor(id: number, name: string, createdAt: string, updatedAt: string, _role: string, themeStoreId: number | null, previewable: boolean, processing: boolean, adminGraphqlApiId: string);
    get role(): string;
    set role(_role: string);
}
