export class Theme {
    constructor(id, name, createdAt, updatedAt, _role, themeStoreId, previewable, processing, adminGraphqlApiId) {
        this.id = id;
        this.name = name;
        this.createdAt = createdAt;
        this.updatedAt = updatedAt;
        this._role = _role;
        this.themeStoreId = themeStoreId;
        this.previewable = previewable;
        this.processing = processing;
        this.adminGraphqlApiId = adminGraphqlApiId;
    }
    get role() {
        if (this._role === 'main') {
            return 'live';
        }
        else {
            return this._role;
        }
    }
    set role(_role) {
        if (_role === 'live') {
            this._role = 'main';
        }
        else {
            this._role = _role;
        }
    }
}
//# sourceMappingURL=theme.js.map