export class Theme {
    constructor(id, name, _role) {
        this.id = id;
        this.name = name;
        this._role = _role;
    }
    get role() {
        if (this._role === 'main') {
            return 'live';
        }
        else {
            return this._role;
        }
    }
    set role(_role) {
        if (_role === 'live') {
            this._role = 'main';
        }
        else {
            this._role = _role;
        }
    }
}
//# sourceMappingURL=theme.js.map